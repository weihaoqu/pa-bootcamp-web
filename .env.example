# Admin credentials for the instructor dashboard
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# NextAuth secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your_random_secret
NEXTAUTH_URL=http://localhost:3000

# Enrollment code students need to register
CLASS_JOIN_CODE=changeme

# SQLite database path (default: ./pa-bootcamp.db)
# DB_PATH=/data/pa-bootcamp.db
